# Legacy prototype code — DO NOT USE

This folder contains the original prototype code from the initial version of the project.

It is kept here for **historical reference only** and is not part of the active codebase.
All active code lives in `src/`.

See `CHANGELOG.md` for context on what changed and why.
